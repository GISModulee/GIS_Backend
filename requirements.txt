fastapi==0.115.6
uvicorn[standard]==0.34.0

sqlalchemy==2.0.36
alembic==1.14.0
geoalchemy2==0.17.0
psycopg2-binary==2.9.10

pydantic[email]==2.10.4
pydantic-settings==2.7.1

python-dotenv==1.0.1
python-multipart==0.0.20

shapely==2.0.6
geojson==3.1.0
httpx==0.28.1

pillow==11.1.0
torch==2.5.1
transformers==4.37.2
geoclip==1.2.0

# python-magic is a wrapper around the native libmagic C library and both
# providers expose the same `magic` module, so exactly ONE is installed per
# platform (no conflicts):
#   - Windows: python-magic-bin bundles the required libmagic DLL.
#   - Linux/macOS: python-magic wraps the system libmagic (see README).
python-magic-bin==0.4.14; sys_platform == "win32"
python-magic==0.4.27; sys_platform != "win32"

geopandas==1.0.1
rasterio==1.3.11
