// src/components/legend/Legend.jsx
// Shows color swatches for all visible (non-group) layers on the map.
// Returns null when there are no layers so nothing renders.
export default function Legend() {
  // Wire this to your layers state later when layers are added.
  // For now it renders nothing so the map works without errors.
  return null;
}